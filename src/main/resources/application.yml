server:
  port: 4000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/myjpa
    username: amigoscode
    password: password
    driver-class-name: org.postgresql.Driver
#  jpa:
#    show-sql: true
#
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true